  ORG 0000h
  LJMP BEGIN	;LJMP jumps to the next 16bit address
  ORG 000bh
  LJMP ISR
BEGIN:	
  MOV TMOD,#01H
  MOV IE,#82H
  MOV R0,#0AH
  MOV TH0,#73H
  MOV TL0,#97H
  MOV TCON,#10H
ENDLOOP:
  SJMP ENDLOOP
ISR:
  MOV A,R0
  SUBB A,#01H
  MOV R0,A
  JZ LOGIC
  MOV TH0,#73H
  MOV TL0,#97H
  MOV TCON,#10H
  RETI
LOGIC:	
  CPL P1.0
  MOV R0,#0AH
  MOV TH0,#73H
  MOV TL0,#97H
  MOV TCON,#10H
  RETI
